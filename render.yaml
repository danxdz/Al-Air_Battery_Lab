services:
  - type: web
    name: al-air-battery-lab
    env: python
    branch: main
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app:app" --bind 0.0.0.0:$PORT --workers 4
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
